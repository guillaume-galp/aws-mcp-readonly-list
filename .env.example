# AWS Configuration
# The server uses AWS SDK default credential chain
# No need to set AWS_ACCESS_KEY_ID or AWS_SECRET_ACCESS_KEY here
# Use AWS_PROFILE or configure credentials via `aws configure`

# AWS Region (optional, defaults to us-east-1)
AWS_REGION=us-east-1

# AWS Profile (optional, for using specific AWS CLI profiles)
# AWS_PROFILE=your-profile-name

# Optional: AWS Role Assumption
# AWS_ASSUME_ROLE_ARN=arn:aws:iam::123456789012:role/ReadOnlyRole
# AWS_SESSION_DURATION=3600

# Logging Configuration
LOG_LEVEL=info
